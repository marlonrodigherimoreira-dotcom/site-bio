/**
 * Fundo do site — por enquanto limpo, pronto para as próximas
 * adições visuais.
 */
export default function AnimatedBackground() {
  return <div className="site-background" aria-hidden="true" />
}
